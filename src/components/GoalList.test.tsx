import { describe, it } from 'vitest'

describe('GoalList', () => {
  it.todo('Goal 목록을 표시한다')
  it.todo('Goal이 없으면 빈 상태 메시지를 표시한다')
  it.todo('활성 Goal과 비활성 Goal을 다른 스타일로 구분한다')
  it.todo('size prop에 따라 컴팩트/기본 크기로 렌더링한다')
})
